---
name: Bug report
about: Something isn't working correctly
title: "[BUG] "
labels: bug
---

**Component**
Which part is affected? (`linux/`, `core/`, `cli/`, `gui/`, `packaging/`, `docs/`)

**Environment**
- OS/distro (if `linux/` related): 
- Desktop Environment (if relevant): GNOME / KDE / XFCE / other
- Python version (if `core/`/`cli/` related): 
- Browser (if `gui/` related): 

**What happened**
A clear description of the bug.

**What you expected**
What you expected to happen instead.

**Steps to reproduce**
1. 
2. 
3. 

**Relevant output**
```
paste command output, or linux/logs/install.log contents, here
```

**Additional context**
Anything else relevant (layout JSON you were using, etc.)
